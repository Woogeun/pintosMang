*
next_thread_to_run에서 pop할때 pop_front말고?
혹은 insert할때 ordered로
단, 이때 lock되어있는건 제외(Lock에서 자동으로 제외되나?)


*
thread_create() next_thread_to_run() - thread.c
sema_up(), lock_acquire(), cond_signal() - synch.c

*
thread + = effective prior + real prior

*
4cases donation
- simple, nested, multiple(2ver), deffered

*
thread_set_priority()

*
nesting level <= 8

*
higher priority 들어오면 바로 yield로 양보해주기

*
loop에서 sema_try_down/lock_try_acquire 은 코스트가 크다